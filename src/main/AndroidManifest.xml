<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Startup">

        <activity android:name=".ConfigActivity" />
        <activity android:name=".CriarActivity" />
        <activity android:name=".ContaActivity" />
        <activity android:name=".NotificacoesActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ConversasActivity" />
        <activity android:name=".ProcurarActivity" />
        <activity android:name=".FeedActivity" />
        <activity android:name=".CadastroActivity" />
        <activity android:name=".VerificacaoActivity" />
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
